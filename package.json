{
  "name": "harry-lang",
  "version": "1.0.0",
  "description": "",
  "main": "execute.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "./node_modules/.bin/babel  parser.js --out-file parser-compiled.js -w && ./node_modules/.bin/babel execute.js -out-file execute-compiled.js -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.23.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0"
  }
}
